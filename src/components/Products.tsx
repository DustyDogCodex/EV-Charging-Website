function Products() {
  return (
    <section
        id="products"
        className="h-screen flex items-center"
    >
        <h2>Wide selection of products to fit every need</h2>

    </section>
  )
}

export default Products